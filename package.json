{
  "name": "ai-cover-letter-filler",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:ext": "echo \"Dev server stub (Vite to be added)\"",
    "build:ext": "rm -rf extension/dist && mkdir -p extension/dist && cp -r extension/manifest.json extension/background.js extension/content.js extension/options extension/shared extension/dist/ && echo \"Built to extension/dist\" && find extension/dist -maxdepth 2 -type f -print",
    "run:pipeline": "node scripts/run_pipeline.mjs",
    "inspect:template": "node scripts/inspect_template.mjs"
  },
  "devDependencies": {
  },
  "dependencies": {
    "jsdom": "^24.1.0",
    "node-fetch": "^3.3.2",
    "@supabase/supabase-js": "^2.45.4",
    "mammoth": "^1.6.0",
    "pdf-parse": "^1.1.1"
  }
}


