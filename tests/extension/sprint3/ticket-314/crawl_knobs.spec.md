# TICKET-314 — Crawl delay and page cap knobs

Checks:
- [x] `options.defaultDelaySeconds` respected when robots has no delay
- [x] `maxPages` limits total pages fetched
- [x] Returned result indicates whether cap was reached


