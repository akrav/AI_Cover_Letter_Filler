# TICKET-307 — Evidence store

Checks:
- [x] `makeRecord` conforms to schema (variable_key, quote, url, source_title, score, created_at)
- [x] Save/load roundtrip via `chrome.storage.local`
- [x] Stored under key `evidence:<jobSessionId>`


