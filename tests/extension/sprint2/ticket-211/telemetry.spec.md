# TICKET-211 — Telemetry stub checks

Expectations:
- [x] When `telemetryOptIn` is false → events dropped
- [x] When `telemetryOptIn` is true → events appended to `telemetryEvents` array
- [x] Options page can export and clear telemetry JSON


