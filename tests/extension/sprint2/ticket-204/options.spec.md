# TICKET-204 — Options page checks

Expectations:
- [x] Options page at `options/options.html`
- [x] Saves `apiKey`, `model`, `budgetCap`, `strictness` to `chrome.storage.local`
- [x] Budget validation prevents negative/NaN values
- [x] API key is not logged and masked on load


