# TICKET-229 — Offline / cached-only mode

Checks:
- [x] Options includes “Offline / cached-only mode”
- [x] When enabled, panel shows “Status: Ready (Offline)”
- [x] Network features are expected to be disabled (use cached data only)


