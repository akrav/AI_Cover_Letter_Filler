# TICKET-401 — Style profile builder

Checks:
- [x] `buildStyleProfile(samples[])` returns schema fields (tone, cadence, structure, lexicon, formatting)
- [x] Includes `exemplars` few-shot snippets
- [x] Stable descriptors across runs for same inputs


